import { Card } from "../card"
import './listaTime.css';
export const ListaTime = (props) => {
    
    return (
        <div>
            <h1>{props.nome}</h1>
            <div className="container-time">
            
            </div>
             </div>
    )
}

